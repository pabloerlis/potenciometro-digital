from ws_connection import ClientClosedErrorfrom ws_multiserver import WebSocketMultiServerimport _threadclass TestServer(WebSocketMultiServer):     def __init__(self):        super().__init__("index.html", 10)#a página a ser servida os arquivos serão servidos também; fotos, conteúdo dentro de outras pastas, etc.    def _make_client(self, conn):        return server = TestServer()server.start()def startWeb():     global server    try:        while True:            server.process_all()    except KeyboardInterrupt:        pass    server.stop()startWeb = _thread.start_new_thread(startWeb,())